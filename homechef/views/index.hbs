<!DOCTYPE html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  
  <link rel="stylesheet" href="/css/baseStyle.css" />
  <link rel="stylesheet" href="/css/nav.css" />
  
</head>

<body onload="setActiveNavElem()">
  <div>
  {{>navbar}}
  </div> 
  {{#if isHomeView }}
  {{>homeUser}}
  {{/if}}

  {{#if isRecipesView }}
  {{>recipeList}}
  {{/if}}

  {{#if isRecipeView }}
  {{>recipe}}
  {{/if}}

  {{#if isRecipeForm }}
  {{>createRecipe}}
  {{/if}}
  
  <script src="/js/index.js" ></script>
</body>